[
  {
    "objectID": "submodules-plan.html",
    "href": "submodules-plan.html",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "Future-reference plan for converting the four sibling-directory symlinks in the yelmox root (fesm-utils, FastIsostasy, rembo1, yelmo) into proper git submodules. Not yet executed.\n\n\nHave yelmox pin specific commits of each component repo and clone them automatically as submodules under the existing paths, replacing the current symlinks to sibling clones under /Users/alrobi001/models/.\n\n\n\nyelmox root contains symlinks to sibling clones:\n\n\n\n\n\n\n\nsymlink\nremote\n\n\n\n\nfesm-utils\ngit@github.com:fesmc/fesm-utils.git\n\n\nFastIsostasy\ngit@github.com:palma-ice/FastIsostasy.git\n\n\nrembo1\ngit@github.com:alex-robinson/rembo1.git\n\n\nyelmo\ngit@github.com:fesmc/yelmo.git\n\n\nice_data\n(leave as symlink — not in scope)\n\n\n\nThese names are listed in .gitignore. The Makefile already uses inner paths like fesm-utils/utils/include-serial, FastIsostasy/libisostasy/include, yelmo/libyelmo/include, rembo1/librembo/include, which will be preserved by submodules.\n\n\n\n\nURLs: SSH form, matching current remotes. For yelmo use git@github.com:fesmc/yelmo.git (not the palma-ice mirror).\nBranch to track: main for all four (default branch on each origin).\n.gitmodules form: pin to commit only — no branch = … line. Bumping the pin is an explicit git add &lt;submodule&gt; in yelmox.\nInitial pin: tip of origin/main at the time of conversion. Re-fetch and re-check tips before running.\nice_data: untouched.\n\n\n\n\nFrom the yelmox root:\n# 1. Remove symlinks (no data lost — sibling clones at /Users/alrobi001/models/&lt;name&gt; remain)\nrm fesm-utils FastIsostasy rembo1 yelmo\n\n# 2. Drop the four entries from .gitignore (paths will now be tracked as submodules)\n#    Keep: ice_data, libyelmox, unit_tests, libs/lis*, coordinates, isostasy_data, etc.\n\n# 3. Add each submodule (clones fresh into yelmox/&lt;path&gt;)\ngit submodule add git@github.com:fesmc/fesm-utils.git       fesm-utils\ngit submodule add git@github.com:palma-ice/FastIsostasy.git FastIsostasy\ngit submodule add git@github.com:alex-robinson/rembo1.git   rembo1\ngit submodule add git@github.com:fesmc/yelmo.git            yelmo\n\n# 4. Verify .gitmodules + each submodule pin, then single commit:\ngit commit -m \"add submodules: fesm-utils, FastIsostasy, rembo1, yelmo\"\n\n\n\nEach submodule contributes only its source. The build artifacts the Makefile depends on (fesm-utils/fftw-serial/, fesm-utils/lis-serial/, FastIsostasy/libisostasy/include/, yelmo/libyelmo/include/, rembo1/librembo/include/) are gitignored inside each submodule and must be built per the component’s own install/build process — the same as today.\n\n\n\ngit config --global push.recurseSubmodules check\nSo git push in yelmox refuses if any submodule has unpushed commits at its pinned sha. Catches the easy mistake of pushing yelmox with a yelmo pin nobody else can fetch.\n\n\n\ncd yelmox/yelmo\ngit checkout main                   # ALWAYS — fresh clones land in detached HEAD\n# edit, build via yelmox Makefile, iterate\ngit commit -am \"...\"\ngit push                            # to fesmc/yelmo\n\ncd ..                               # back to yelmox root\ngit status                          # shows: modified: yelmo (new commits)\ngit add yelmo                       # bump the pin\ngit commit -m \"bump yelmo to &lt;sha&gt;: &lt;reason&gt;\"\nAfter a fresh git clone --recurse-submodules of yelmox elsewhere, put all submodules on their branches in one shot:\ngit submodule foreach 'git checkout main || git checkout master'\n\n\nIf a yelmox change needs a matching yelmo change: push the yelmo branch first, then in yelmox commit the gitlink bump together with the yelmox-side code that uses the new API — that single yelmox commit becomes the atomic unit. push.recurseSubmodules = check enforces the ordering.\n\n\n\nAfter conversion there are two clones of (e.g.) yelmo on disk: the original at /Users/alrobi001/models/yelmo and the submodule at /Users/alrobi001/models/yelmox/yelmo. They drift unless synced via the remote. Three options at conversion time:\n\n(a) Abandon the siblings; develop only inside the submodule. Simplest mental model.\n(b) Keep the siblings as the primary workspace; push from sibling and git pull inside the submodule. Two extra commands per sync but reuses one dev tree across multiple yelmox checkouts/worktrees.\n(c) Point the submodule at the sibling locally with git config submodule.&lt;name&gt;.url /Users/alrobi001/models/&lt;name&gt; (then git submodule sync). Submodule fetches from the local path; .gitmodules still points at GitHub for everyone else. Useful for one-yelmo-feeds-many-yelmox setups.\n\n\n\n\n\n\nfesm-utils local clone has uncommitted edits on alex-dev to install.sh, utils/src/esm.f90, utils/test/test_esm.nml, plus untracked fesm-utils/, utils/src/esm_base.f90, utils/src/esm_extra.f90. These are not on the origin/main tip and will not be in the initial pin. Decide before conversion whether to land them on main first.\nAfter conversion, the fesm-utils + FastIsostasy alex-dev branches still exist and are ahead of/behind main in various ways — decide separately whether to retire those branches or keep them as long-lived dev lines.\n\n\n\n\nFor reference only; re-fetch before executing.\n\n\n\n\n\n\n\n\nsubmodule\nsha\nmessage\n\n\n\n\nfesm-utils\nf7a3917\nMerge PR #4 from fesmc/alex-dev\n\n\nFastIsostasy\n2969564\nMerge PR #46 from palma-ice/adaptive-dt\n\n\nrembo1\naa999a9\nUpdated runme.\n\n\nyelmo\nb3e233f\ndocs: move Margin-front mass balance …"
  },
  {
    "objectID": "submodules-plan.html#goal",
    "href": "submodules-plan.html#goal",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "Have yelmox pin specific commits of each component repo and clone them automatically as submodules under the existing paths, replacing the current symlinks to sibling clones under /Users/alrobi001/models/."
  },
  {
    "objectID": "submodules-plan.html#current-state",
    "href": "submodules-plan.html#current-state",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "yelmox root contains symlinks to sibling clones:\n\n\n\n\n\n\n\nsymlink\nremote\n\n\n\n\nfesm-utils\ngit@github.com:fesmc/fesm-utils.git\n\n\nFastIsostasy\ngit@github.com:palma-ice/FastIsostasy.git\n\n\nrembo1\ngit@github.com:alex-robinson/rembo1.git\n\n\nyelmo\ngit@github.com:fesmc/yelmo.git\n\n\nice_data\n(leave as symlink — not in scope)\n\n\n\nThese names are listed in .gitignore. The Makefile already uses inner paths like fesm-utils/utils/include-serial, FastIsostasy/libisostasy/include, yelmo/libyelmo/include, rembo1/librembo/include, which will be preserved by submodules."
  },
  {
    "objectID": "submodules-plan.html#decisions-made",
    "href": "submodules-plan.html#decisions-made",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "URLs: SSH form, matching current remotes. For yelmo use git@github.com:fesmc/yelmo.git (not the palma-ice mirror).\nBranch to track: main for all four (default branch on each origin).\n.gitmodules form: pin to commit only — no branch = … line. Bumping the pin is an explicit git add &lt;submodule&gt; in yelmox.\nInitial pin: tip of origin/main at the time of conversion. Re-fetch and re-check tips before running.\nice_data: untouched."
  },
  {
    "objectID": "submodules-plan.html#steps-to-execute",
    "href": "submodules-plan.html#steps-to-execute",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "From the yelmox root:\n# 1. Remove symlinks (no data lost — sibling clones at /Users/alrobi001/models/&lt;name&gt; remain)\nrm fesm-utils FastIsostasy rembo1 yelmo\n\n# 2. Drop the four entries from .gitignore (paths will now be tracked as submodules)\n#    Keep: ice_data, libyelmox, unit_tests, libs/lis*, coordinates, isostasy_data, etc.\n\n# 3. Add each submodule (clones fresh into yelmox/&lt;path&gt;)\ngit submodule add git@github.com:fesmc/fesm-utils.git       fesm-utils\ngit submodule add git@github.com:palma-ice/FastIsostasy.git FastIsostasy\ngit submodule add git@github.com:alex-robinson/rembo1.git   rembo1\ngit submodule add git@github.com:fesmc/yelmo.git            yelmo\n\n# 4. Verify .gitmodules + each submodule pin, then single commit:\ngit commit -m \"add submodules: fesm-utils, FastIsostasy, rembo1, yelmo\""
  },
  {
    "objectID": "submodules-plan.html#post-conversion-build-steps-still-required",
    "href": "submodules-plan.html#post-conversion-build-steps-still-required",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "Each submodule contributes only its source. The build artifacts the Makefile depends on (fesm-utils/fftw-serial/, fesm-utils/lis-serial/, FastIsostasy/libisostasy/include/, yelmo/libyelmo/include/, rembo1/librembo/include/) are gitignored inside each submodule and must be built per the component’s own install/build process — the same as today."
  },
  {
    "objectID": "submodules-plan.html#recommended-config-one-time-global",
    "href": "submodules-plan.html#recommended-config-one-time-global",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "git config --global push.recurseSubmodules check\nSo git push in yelmox refuses if any submodule has unpushed commits at its pinned sha. Catches the easy mistake of pushing yelmox with a yelmo pin nobody else can fetch."
  },
  {
    "objectID": "submodules-plan.html#development-workflow-on-a-submodule",
    "href": "submodules-plan.html#development-workflow-on-a-submodule",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "cd yelmox/yelmo\ngit checkout main                   # ALWAYS — fresh clones land in detached HEAD\n# edit, build via yelmox Makefile, iterate\ngit commit -am \"...\"\ngit push                            # to fesmc/yelmo\n\ncd ..                               # back to yelmox root\ngit status                          # shows: modified: yelmo (new commits)\ngit add yelmo                       # bump the pin\ngit commit -m \"bump yelmo to &lt;sha&gt;: &lt;reason&gt;\"\nAfter a fresh git clone --recurse-submodules of yelmox elsewhere, put all submodules on their branches in one shot:\ngit submodule foreach 'git checkout main || git checkout master'\n\n\nIf a yelmox change needs a matching yelmo change: push the yelmo branch first, then in yelmox commit the gitlink bump together with the yelmox-side code that uses the new API — that single yelmox commit becomes the atomic unit. push.recurseSubmodules = check enforces the ordering.\n\n\n\nAfter conversion there are two clones of (e.g.) yelmo on disk: the original at /Users/alrobi001/models/yelmo and the submodule at /Users/alrobi001/models/yelmox/yelmo. They drift unless synced via the remote. Three options at conversion time:\n\n(a) Abandon the siblings; develop only inside the submodule. Simplest mental model.\n(b) Keep the siblings as the primary workspace; push from sibling and git pull inside the submodule. Two extra commands per sync but reuses one dev tree across multiple yelmox checkouts/worktrees.\n(c) Point the submodule at the sibling locally with git config submodule.&lt;name&gt;.url /Users/alrobi001/models/&lt;name&gt; (then git submodule sync). Submodule fetches from the local path; .gitmodules still points at GitHub for everyone else. Useful for one-yelmo-feeds-many-yelmox setups."
  },
  {
    "objectID": "submodules-plan.html#open-follow-ups-deferred",
    "href": "submodules-plan.html#open-follow-ups-deferred",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "fesm-utils local clone has uncommitted edits on alex-dev to install.sh, utils/src/esm.f90, utils/test/test_esm.nml, plus untracked fesm-utils/, utils/src/esm_base.f90, utils/src/esm_extra.f90. These are not on the origin/main tip and will not be in the initial pin. Decide before conversion whether to land them on main first.\nAfter conversion, the fesm-utils + FastIsostasy alex-dev branches still exist and are ahead of/behind main in various ways — decide separately whether to retire those branches or keep them as long-lived dev lines."
  },
  {
    "objectID": "submodules-plan.html#pins-captured-at-planning-time-2026-05-21",
    "href": "submodules-plan.html#pins-captured-at-planning-time-2026-05-21",
    "title": "Submodule conversion plan for yelmox",
    "section": "",
    "text": "For reference only; re-fetch before executing.\n\n\n\n\n\n\n\n\nsubmodule\nsha\nmessage\n\n\n\n\nfesm-utils\nf7a3917\nMerge PR #4 from fesmc/alex-dev\n\n\nFastIsostasy\n2969564\nMerge PR #46 from palma-ice/adaptive-dt\n\n\nrembo1\naa999a9\nUpdated runme.\n\n\nyelmo\nb3e233f\ndocs: move Margin-front mass balance …"
  },
  {
    "objectID": "configme-install-details.html",
    "href": "configme-install-details.html",
    "title": "What configme install yelmox does",
    "section": "",
    "text": "What configme install yelmox does\nThis page is for context only. The recommended way to install YelmoX is to run configme install yelmox (see the Quick start); you do not need to run any of the commands below by hand. They are reproduced here so you can see exactly what configme clones, configures, links, and builds on your behalf.\nconfigme install performs the following phases:\n\nClone the YelmoX checkout and each component repository it needs (fesm-utils, coordinates, yelmo, FastIsostasy, rembo1).\nManifest — write .configme/manifest.toml so the checkout is self-describing regardless of its directory name.\nLink each component into the directories where the build expects it.\nConfigure — generate a system-specific Makefile for every package from shared machine/compiler fragments, with the netCDF paths detected automatically.\nBuild fesm-utils (LIS + FFTW + utils). This is the slow step (~10-30 min), which is why it is folded into the install rather than run separately.\nExtras — install runme if it is missing, create .runme_config, and link the external data paths (ice_data, isostasy_data).\n\nEach run also writes the exact commands it executed to a .install.sh script in the checkout, so any install is reproducible. The script below is the equivalent for the dkrz_levante machine with the ifx compiler; substitute your own machine and compiler, and note that the netCDF paths (NC_FROOT / NC_CROOT) and the install root are filled in automatically for your system.\n#!/usr/bin/env bash\n# Generated by `configme install` — reproduces this install.\nset -euo pipefail\n# target=yelmox  machine=dkrz_levante  compiler=ifx\n\n# --- clone ---\ngit clone git@github.com:fesmc/yelmox.git $YELMOXROOT\ngit clone git@github.com:fesmc/fesm-utils.git $YELMOXROOT/fesm-utils\n# fesm-utils/utils: component of fesm-utils (not cloned)\ngit clone git@github.com:fesmc/coordinates.git $YELMOXROOT/coordinates\ngit clone git@github.com:fesmc/yelmo.git $YELMOXROOT/yelmo\ngit clone git@github.com:palma-ice/FastIsostasy.git $YELMOXROOT/FastIsostasy\ngit clone git@github.com:alex-robinson/rembo1.git $YELMOXROOT/rembo1\n\n# --- manifest ---\nmkdir -p $YELMOXROOT/.configme  # write .configme/manifest.toml\n\n# --- links ---\nln -s ../fesm-utils $YELMOXROOT/yelmo/fesm-utils\nln -s ../fesm-utils $YELMOXROOT/FastIsostasy/fesm-utils\nln -s ../../coordinates $YELMOXROOT/rembo1/libs/coordinates\n\n# --- configure (per package) ---\n# fesm-utils: build with autotools (slow, ~10-30 min):\n(cd $YELMOXROOT/fesm-utils && NC_FROOT=&lt;auto-detected&gt; NC_CROOT=&lt;auto-detected&gt; ./build.py --variant both -m dkrz_levante -c ifx)\n(cd $YELMOXROOT/fesm-utils/utils && configme config fesm-utils/utils -m dkrz_levante -c ifx)\n# fesm-utils/utils: build (serial, omp):\n(cd $YELMOXROOT/fesm-utils/utils && make openmp=0 fesmutils-static)\n(cd $YELMOXROOT/fesm-utils/utils && make openmp=1 fesmutils-static)\n(cd $YELMOXROOT/coordinates && configme config coordinates -m dkrz_levante -c ifx)\n(cd $YELMOXROOT/yelmo && configme config yelmo -m dkrz_levante -c ifx)\n(cd $YELMOXROOT/FastIsostasy && configme config FastIsostasy -m dkrz_levante -c ifx)\n(cd $YELMOXROOT/rembo1 && configme config rembo1 -m dkrz_levante -c ifx)\n(cd $YELMOXROOT && configme config yelmox -m dkrz_levante -c ifx)\n\n# --- extras ---\ncommand -v runme &gt;/dev/null 2&gt;&1 || pip install git+https://github.com/fesmc/runme\n# runme_config: create .runme_config and set hpc/account\nrunme --config  # or: cp .runme/runme_config .runme_config\n# data_link ice_data: ln -s &lt;path&gt; ice_data\n# data_link isostasy_data: ln -s &lt;path&gt; isostasy_data"
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "YelmoX",
    "section": "",
    "text": "YelmoX is the driver framework around the Yelmo ice-sheet model. It bundles Yelmo together with the libraries needed to run realistic ice-sheet simulations: climate and ocean forcing modules, the FastIsostasy bedrock module, the REMBOv1 energy/moisture-balance model, and shared utilities from fesm-utils.\nFor documentation of the Yelmo ice-sheet model itself, see the Yelmo docs.\n\n\nThe YelmoX code repository is here: https://github.com/fesmc/yelmox. To get a local copy:\ngit clone git@github.com:fesmc/yelmox.git\ncd yelmox\n\n\n\nYelmoX is configured and built with configme, a small Python tool that detects your netCDF installation, configures every package in the stack for your machine and compiler, and clones/links/builds the whole thing with one command. It is installed once, globally, and provides the configme command on your PATH:\npip install git+https://github.com/fesmc/configme\nTo upgrade it later, add --upgrade to the same command. If the configme command is not found afterwards, your Python user bin directory is probably not on your PATH; add it in your ~/.bashrc / ~/.zshrc:\nexport PATH=\"${PATH}:${HOME}/.local/bin\"\nThe only system dependency you must install yourself is netCDF (see the Yelmo dependencies notes for installation tips). Everything else — LIS, FFTW, the fesm-utils libraries, the component repositories, their links, and runme — is managed by configme.\n\n\n\nWith configme installed, build the whole YelmoX stack with a single command from the directory where you want the checkout to live:\nconfigme install yelmox\nThis clones YelmoX and its component repositories (fesm-utils, coordinates, yelmo, FastIsostasy, rembo1), configures each for your machine and compiler, links them into the YelmoX directory, and builds fesm-utils (LIS + FFTW + utils, which can take 10-30 min). If configme can detect your machine from the hostname it does so, otherwise it prompts you.\nCommon options:\nconfigme install yelmox -m dkrz_levante -c ifx   # pick the machine + compiler explicitly\nconfigme install yelmox -d https                 # clone over HTTPS (no GitHub SSH key needed)\nconfigme install yelmox --dir ~/models/yelmox    # put the checkout here instead of ./yelmox\nconfigme install yelmox --overwrite              # re-clone over an existing checkout\nconfigme install yelmox --build-deps             # rebuild dependency packages without prompting\nRun configme list for the supported machines and compilers, and configme --help for the full command surface. The exact clone/configure/link/build commands configme install yelmox runs for you are recorded in a .install.sh script in the checkout, and are also shown for context on the configme install details page — these are shown for reference only; configme install is the recommended path and you do not need to run them by hand.\nThat’s it, you should now be ready to compile and run any yelmox program flavor:\nmake clean\nmake yelmox"
  },
  {
    "objectID": "index.html#how-to-get-yelmox",
    "href": "index.html#how-to-get-yelmox",
    "title": "YelmoX",
    "section": "",
    "text": "The YelmoX code repository is here: https://github.com/fesmc/yelmox. To get a local copy:\ngit clone git@github.com:fesmc/yelmox.git\ncd yelmox"
  },
  {
    "objectID": "index.html#install-configme-one-time",
    "href": "index.html#install-configme-one-time",
    "title": "YelmoX",
    "section": "",
    "text": "YelmoX is configured and built with configme, a small Python tool that detects your netCDF installation, configures every package in the stack for your machine and compiler, and clones/links/builds the whole thing with one command. It is installed once, globally, and provides the configme command on your PATH:\npip install git+https://github.com/fesmc/configme\nTo upgrade it later, add --upgrade to the same command. If the configme command is not found afterwards, your Python user bin directory is probably not on your PATH; add it in your ~/.bashrc / ~/.zshrc:\nexport PATH=\"${PATH}:${HOME}/.local/bin\"\nThe only system dependency you must install yourself is netCDF (see the Yelmo dependencies notes for installation tips). Everything else — LIS, FFTW, the fesm-utils libraries, the component repositories, their links, and runme — is managed by configme."
  },
  {
    "objectID": "index.html#quick-start",
    "href": "index.html#quick-start",
    "title": "YelmoX",
    "section": "",
    "text": "With configme installed, build the whole YelmoX stack with a single command from the directory where you want the checkout to live:\nconfigme install yelmox\nThis clones YelmoX and its component repositories (fesm-utils, coordinates, yelmo, FastIsostasy, rembo1), configures each for your machine and compiler, links them into the YelmoX directory, and builds fesm-utils (LIS + FFTW + utils, which can take 10-30 min). If configme can detect your machine from the hostname it does so, otherwise it prompts you.\nCommon options:\nconfigme install yelmox -m dkrz_levante -c ifx   # pick the machine + compiler explicitly\nconfigme install yelmox -d https                 # clone over HTTPS (no GitHub SSH key needed)\nconfigme install yelmox --dir ~/models/yelmox    # put the checkout here instead of ./yelmox\nconfigme install yelmox --overwrite              # re-clone over an existing checkout\nconfigme install yelmox --build-deps             # rebuild dependency packages without prompting\nRun configme list for the supported machines and compilers, and configme --help for the full command surface. The exact clone/configure/link/build commands configme install yelmox runs for you are recorded in a .install.sh script in the checkout, and are also shown for context on the configme install details page — these are shown for reference only; configme install is the recommended path and you do not need to run them by hand.\nThat’s it, you should now be ready to compile and run any yelmox program flavor:\nmake clean\nmake yelmox"
  }
]